name: ditto_interface
description: The common API for clipboard platform plugins
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  rows_lint: 0.1.1
